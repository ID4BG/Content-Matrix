kind = "web"
previewPath = "/"
title = "Content Hub"
version = "1.0.0"
id = "artifacts/content-hub"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18450

[services.development]
run = "pnpm --filter @workspace/content-hub run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/content-hub", "run", "build" ]
publicDir = "artifacts/content-hub/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18450"
BASE_PATH = "/"
